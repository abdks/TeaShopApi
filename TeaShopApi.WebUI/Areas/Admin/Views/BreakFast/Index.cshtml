@model List<TeaShopApi.WebUI.Dtos.BreakFastDtos.ResultBreakFastDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
}

<h1>Index</h1>


<div class="row">
	<!-- [ sample-page ] start -->
	<div class="col-sm-12">
		<div class="card">

			<div class="card-header">
				<h5>Menü Sayfası</h5>
			</div>
			<div class="card-body">
				<table class="table table-hover">
					<tr>
						<th>#</th>
						<th>Adı</th>
						<th>Resimi</th>
						<th>Açıklaması</th>
						<th>Fiyatı</th>
						<th>Sil</th>
						<th>Güncelle</th>
					</tr>
					@foreach (var item in Model)
					{
						<tr>
							<td>@item.BreakFastID</td>
							<td>@item.Title</td>
							<td>@item.ImageUrl</td>
							<td>@item.Description</td>
							<td>@item.Price</td>
							<td><a href="/Admin/BreakFast/DeleteBreakFast/@item.BreakFastID" class="btn btn-outline-danger">Sil</a></td>
							<td><a href="/Admin/BreakFast/UpdateBreakFast/@item.BreakFastID" class="btn btn-outline-success">Güncelle</a></td>
						</tr>
					}
				</table>
				<a href="/Admin/BreakFast/CreateBreakFast" class="btn btn-outline-primary">Yeni Kahvaltı Girişi</a>

			</div>
		</div>
	</div>
	<!-- [ sample-page ] end -->
</div>